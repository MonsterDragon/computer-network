前面讨论的是节点时延，即单台路由器上的时延。我们现在考虑从源到目的地的总时延。
假定在源主机和目的主机之间有N-1台路由器。我们还要假设该网络此时是无拥塞的（因此排毒誓言是微不足道的），在每台路由器和源主机上的处理时延是d(proc)，每台路由器和源主机的输出速率是Rbps，每条链路的传播时延是d(prop)。则端到端时延是：
d(end-end) = N(d(proc) + d(trans) + d(prop))
d(trans) = L/R
